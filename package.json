{
  "name": "shrimpia-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "turbo start",
    "test": "turbo test",
    "lint": "turbo lint",
    "deploy": "turbo deploy"
  },
  "devDependencies": {
    "turbo": "^1.10.14"
  }
}
